# Claude 工作指南

## 语言优先级

**优先使用英文** - 除非特殊说明，所有内容优先采用英文

**例外情况：**
- 文章内容描述（用户已有的中文文章保持原样）
- 用户明确要求使用中文

**适用范围：**
- About 页面
- Links 页面
- 配置文件注释
- 文档说明
- 代码注释

---

## 网络请求规则

### ❌ 不可用
- `WebFetch` - 网络原因无法使用

### ✅ 可用方案
- `WebSearch` - 内置搜索，正常使用
- `curl` + 代理 - 用于获取网页内容

### Curl 代理配置

```bash
curl -x http://127.0.0.1:7897 -L <URL> -H "User-Agent: Mozilla/5.0"
```

**示例：**
```bash
curl -x http://127.0.0.1:7897 -L https://example.com -H "User-Agent: Mozilla/5.0"
```

---

**代理端口：** 7897
